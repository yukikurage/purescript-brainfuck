{
  "name": "purescript-brainfuck",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle:ts": "npx tsc -p \"./\" ",
    "bundle:purs": "npx spago bundle --outfile index.js --platform node --minify",
    "bundle": "npm run bundle:ts && npm run bundle:purs"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@assemblyscript/loader": "^0.27.31",
    "@wasmer/sdk": "^0.9.0",
    "binaryen": "^120.0.0",
    "esbuild": "^0.24.0",
    "purescript": "^0.15.15",
    "purs-tidy": "^0.11.0",
    "spago": "^0.93.41",
    "typescript": "^5.6.3"
  }
}
